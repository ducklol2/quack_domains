{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:1": {
        "version": "latest",
        "moby": true
    }
  },
  "postStartCommand": "sudo apt-get update && sudo apt-get install -y avahi-daemon && sudo service dbus start && sudo avahi-daemon -D"
}
